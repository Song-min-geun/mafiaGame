global:
  scrape_interval: 5s # 5초마다 데이터 수집 (부하 테스트용이므로 짧게 설정)

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080'] # docker-compose 내의 app 서비스 이름과 포트