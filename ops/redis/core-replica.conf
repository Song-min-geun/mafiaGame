bind 0.0.0.0
protected-mode no
port 6379

replicaof core-redis-master 6379
replica-read-only yes

appendonly yes
dir /data

# Main business Redis instance: 80% of the local Redis memory budget.
maxmemory 800mb
maxmemory-policy noeviction
